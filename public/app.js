(()=>{
const $=s=>document.querySelector(s), BACK='https://cards.scryfall.io/back.png';
const st={cards:{},deck:[],hand:[],cmd:[],side:[],tokens:[],discard:[],exile:[],field:[],opp:[],view:'you',life:{you:40,opp:40}};
let seq=0,preview=null,lastTap={},selectedField=new Set(),selectedHand=null;
const toast=t=>{const e=$('#toast');if(!e)return;e.textContent=t;e.classList.add('on');clearTimeout(e.t);e.t=setTimeout(()=>e.classList.remove('on'),1200)};
function make(name,zone,meta={}){const id='c'+(++seq);st.cards[id]={id,name,zone,img:'',faces:[],isDoubleFaced:false,stateIndex:0,faceDown:zone==='deck',x:40,y:40,tap:false,p1:0,p:null,t:null,meta:{...meta}};return id}
async function load(c){if(!c||(c.img&&c.faces.length))return;try{let q='/api/card?name='+encodeURIComponent(c.name);if(c.meta?.scryfallId)q+='&id='+encodeURIComponent(c.meta.scryfallId);const r=await fetch(q);if(!r.ok)return;const d=await r.json();c.img=d.image||'';c.faces=Array.isArray(d.faces)&&d.faces.length?d.faces:[{name:c.name,image:c.img}];c.isDoubleFaced=!!d.isDoubleFaced&&c.faces.length>1;c.meta.scryfallId=d.scryfallId||c.meta.scryfallId}catch{}}
const frontImage=c=>c.faces?.[c.stateIndex]?.image||c.img||'';
const displayImage=c=>c.faceDown?BACK:frontImage(c);
const face=c=>{const src=displayImage(c);return src?`<img src="${src}" draggable="false">`:`<div class="cardname">${c.name}</div>`};
function removeFromAll(id){['deck','hand','cmd','side','tokens','discard','exile','field'].forEach(k=>{const i=st[k].indexOf(id);if(i>=0)st[k].splice(i,1)})}
function putInZone(id,zone,atTop=false){const c=st.cards[id];if(!c)return;removeFromAll(id);c.zone=zone;c.tap=false;if(zone==='hand'){c.faceDown=false;st.hand.push(id)}else if(zone==='field')st.field.push(id);else if(zone==='cmd'){c.faceDown=false;(atTop?st.cmd.unshift(id):st.cmd.push(id))}else{const a=st[zone];if(!a)return;(atTop?a.unshift(id):a.push(id)}}
function bringFront(id){const i=st.field.indexOf(id);if(i>=0){st.field.splice(i,1);st.field.push(id)}}
const pointInRect=(x,y,r)=>x>=r.left&&x<=r.right&&y>=r.top&&y<=r.bottom;
function overlapRatio(a,b){const w=Math.max(0,Math.min(a.right,b.right)-Math.max(a.left,b.left)),h=Math.max(0,Math.min(a.bottom,b.bottom)-Math.max(a.top,b.top));return a.width&&a.height?w*h/(a.width*a.height):0}
const fieldRect=()=>$('#field').getBoundingClientRect();
const handHit=(x,y)=>{const h=$('#hand');return h&&pointInRect(x,y,h.getBoundingClientRect())};
const trashHit=(x,y)=>{const t=$('#trash');return t&&pointInRect(x,y,t.getBoundingClientRect())};
function placeOnField(id,x,y,faceDown=null){const c=st.cards[id];if(!c)return;putInZone(id,'field');if(faceDown!==null)c.faceDown=faceDown;const r=fieldRect(),cs=getComputedStyle(document.documentElement),cw=parseFloat(cs.getPropertyValue('--card-w'))||52,ch=parseFloat(cs.getPropertyValue('--card-h'))||73;c.x=Math.max(0,Math.min(100-cw/r.width*100,(x-r.left-cw/2)/r.width*100));c.y=Math.max(0,Math.min(100-ch/r.height*100,(y-r.top-ch/2)/r.height*100));bringFront(id)}
function snapTargetFor(el,c){const a=el.getBoundingClientRect(),zones=[['#discard','discard'],['#deck','deck'],['#exile','exile'],['#tokens','tokens'],['#cmds .cmd','cmd',true]];let best=null,br=0;for(const [sel,zone,commanderOnly] of zones){if(commanderOnly&&!c.meta?.commander)continue;const z=$(sel);if(!z)continue;const r=overlapRatio(a,z.getBoundingClientRect());if(r>=.8&&r>br){best={zone};br=r}}return best}
function revealForZone(c,zone){if(zone==='deck'){c.faceDown=true;return false}c.faceDown=false;return true}
async function moveAndMaybeLoad(id,zone,atTop=false){const c=st.cards[id];putInZone(id,zone,atTop);const shouldLoad=revealForZone(c,zone);if(shouldLoad&&!c.img)await load(c);selectedField.delete(id);if(selectedHand===id)selectedHand=null;render()}
function deleteCards(ids){const list=[...new Set(ids)].filter(id=>st.cards[id]);if(!list.length)return;for(const id of list){removeFromAll(id);delete st.cards[id];selectedField.delete(id);if(selectedHand===id)selectedHand=null}render();toast(list.length>1?`Deleted ${list.length} cards`:'Card deleted')}
function cardRects(){const map=new Map();document.querySelectorAll('#field .card').forEach(el=>map.set(el.dataset.id,el.getBoundingClientRect()));return map}
function overlappingCluster(startId){const rects=cardRects();if(!rects.has(startId))return[startId];const out=new Set([startId]),queue=[startId];while(queue.length){const id=queue.shift(),a=rects.get(id);for(const [other,b] of rects){if(out.has(other))continue;const r1=overlapRatio(a,b),r2=overlapRatio(b,a);if(Math.max(r1,r2)>=.18){out.add(other);queue.push(other)}}}return[...out]}
function selectFieldCluster(id){selectedHand=null;selectedField=new Set(overlappingCluster(id));render();toast(selectedField.size>1?`${selectedField.size} cards locked`:'Card locked')}
function openCard(c,controls=true){if(!c)return;preview=c;$('#pimg').src=displayImage(c)||'';$('#ctrl').classList.toggle('on',controls);$('#state').hidden=!c.isDoubleFaced;$('#inspect').classList.add('on');if(!c.img)load(c).then(()=>{if(preview===c){$('#pimg').src=displayImage(c)||'';$('#state').hidden=!c.isDoubleFaced;render()}})}
function rerenderPreview(){if(preview){$('#pimg').src=displayImage(preview)||'';$('#state').hidden=!preview.isDoubleFaced}}
function cardEl(c,movable=true){
 const d=document.createElement('div');d.className='card'+(c.tap?' tap':'')+(selectedField.has(c.id)?' locked':'');d.dataset.id=c.id;d.style.left=c.x+'%';d.style.top=c.y+'%';d.innerHTML=face(c);
 if(movable&&(c.p1||c.p!==null||c.t!==null)){const b=document.createElement('button');b.className='badge';b.textContent=c.p!==null||c.t!==null?((c.p??0)+c.p1)+'/'+((c.t??0)+c.p1):'+'+c.p1+'/+'+c.p1;b.onclick=e=>{e.stopPropagation();c.p1++;render()};d.appendChild(b)}
 if(!movable){d.onclick=()=>openCard(c,false);return d}
 let drag=null,moved=false,longTimer=null,lockedDrag=false;
 d.onpointerdown=e=>{e.preventDefault();moved=false;lockedDrag=selectedField.has(c.id);bringFront(c.id);const r=fieldRect(),q=d.getBoundingClientRect(),starts={};if(lockedDrag)selectedField.forEach(id=>{const cc=st.cards[id];if(cc)starts[id]={x:cc.x,y:cc.y}});drag={r,dx:e.clientX-q.left,dy:e.clientY-q.top,sx:e.clientX,sy:e.clientY,pid:e.pointerId,starts};longTimer=setTimeout(()=>{if(drag&&!moved){selectFieldCluster(c.id);lockedDrag=true;drag.starts={};selectedField.forEach(id=>{const cc=st.cards[id];if(cc)drag.starts[id]={x:cc.x,y:cc.y}})}},620);d.setPointerCapture?.(e.pointerId)};
 d.onpointermove=e=>{if(!drag)return;const dist=Math.hypot(e.clientX-drag.sx,e.clientY-drag.sy);if(dist>6){moved=true;clearTimeout(longTimer)}if(!moved)return;if(lockedDrag&&selectedField.has(c.id)&&selectedField.size){const dx=(e.clientX-drag.sx)/drag.r.width*100,dy=(e.clientY-drag.sy)/drag.r.height*100;selectedField.forEach(id=>{const cc=st.cards[id],s=drag.starts[id];if(!cc||!s)return;cc.x=Math.max(0,Math.min(94,s.x+dx));cc.y=Math.max(0,Math.min(94,s.y+dy));const el=document.querySelector(`#field .card[data-id="${id}"]`);if(el){el.style.left=cc.x+'%';el.style.top=cc.y+'%'}});return}const x=Math.max(0,Math.min(drag.r.width-d.offsetWidth,e.clientX-drag.r.left-drag.dx)),y=Math.max(0,Math.min(drag.r.height-d.offsetHeight,e.clientY-drag.r.top-drag.dy));c.x=x/drag.r.width*100;c.y=y/drag.r.height*100;d.style.left=c.x+'%';d.style.top=c.y+'%'};
 d.onpointerup=async e=>{clearTimeout(longTimer);if(!drag)return;d.releasePointerCapture?.(drag.pid);const wasLocked=lockedDrag;drag=null;if(moved){if(trashHit(e.clientX,e.clientY)){deleteCards(wasLocked&&selectedField.has(c.id)?[...selectedField]:[c.id]);return}if(wasLocked&&selectedField.has(c.id)&&selectedField.size>1){render();return}if(handHit(e.clientX,e.clientY)){await moveAndMaybeLoad(c.id,'hand');toast('Card returned to hand');return}const snap=snapTargetFor(d,c);if(snap){await moveAndMaybeLoad(c.id,snap.zone,snap.zone==='deck');toast('Snapped to '+(snap.zone==='discard'?'graveyard':snap.zone));return}bringFront(c.id);render();return}if(selectedField.has(c.id)){return}const now=Date.now(),prev=lastTap[c.id]||0;if(now-prev<330){clearTimeout(lastTap[c.id+'_timer']);lastTap[c.id]=0;openCard(c,true)}else{lastTap[c.id]=now;lastTap[c.id+'_timer']=setTimeout(()=>{if(lastTap[c.id]===now){c.tap=!c.tap;lastTap[c.id]=0;render()}},330)}};
 d.onpointercancel=()=>{clearTimeout(longTimer);drag=null};d.oncontextmenu=e=>e.preventDefault();return d
}
function makeGhost(c,forceBack=false,locked=false){const g=document.createElement('div');g.className='drag-ghost'+(locked?' locked':'');const src=forceBack?BACK:displayImage(c);g.innerHTML=src?`<img src="${src}" draggable="false">`:`<span>${c.name}</span>`;document.body.appendChild(g);return g}
function zoneDrag(el,id,from,{forceBack=false,label=''}={}){const c=st.cards[id];if(!c)return;let s=null,ghost=null,moved=false;el.onpointerdown=e=>{e.preventDefault();s={x:e.clientX,y:e.clientY,pid:e.pointerId};moved=false;el.setPointerCapture?.(e.pointerId)};el.onpointermove=e=>{if(!s)return;if(!moved&&Math.hypot(e.clientX-s.x,e.clientY-s.y)>8){moved=true;ghost=makeGhost(c,forceBack)}if(ghost){ghost.style.left=e.clientX+'px';ghost.style.top=e.clientY+'px'}};el.onpointerup=async e=>{if(!s)return;s=null;ghost?.remove();if(!moved){openCard(c,true);return}if(trashHit(e.clientX,e.clientY)){deleteCards([id]);return}if(handHit(e.clientX,e.clientY)){putInZone(id,'hand');c.faceDown=false;if(!c.img)await load(c);render();toast('Moved to hand');return}const r=fieldRect();if(pointInRect(e.clientX,e.clientY,r)){placeOnField(id,e.clientX,e.clientY,forceBack);if(!forceBack&&!c.img)await load(c);render();toast(label||'Moved to battlefield')}};el.onpointercancel=()=>{s=null;ghost?.remove()};el.oncontextmenu=e=>e.preventDefault()}
function handCard(id){
 const c=st.cards[id],d=document.createElement('div');d.className='hcard'+(selectedHand===id?' locked':'');d.dataset.id=id;d.innerHTML=face(c);let s=null,moved=false,scrollMode=false,longTimer=null,ghost=null;
 d.onpointerdown=e=>{e.preventDefault();const row=$('#handrow');s={x:e.clientX,y:e.clientY,pid:e.pointerId,scrollLeft:row.scrollLeft};moved=false;scrollMode=false;longTimer=setTimeout(()=>{if(s&&!moved){selectedField.clear();selectedHand=id;d.classList.add('locked');navigator.vibrate?.(20)}},620);d.setPointerCapture?.(e.pointerId)};
 d.onpointermove=e=>{if(!s)return;const dx=e.clientX-s.x,dy=e.clientY-s.y,row=$('#handrow');if(selectedHand!==id){if(Math.abs(dx)>8&&Math.abs(dx)>Math.abs(dy)){clearTimeout(longTimer);scrollMode=true;row.scrollLeft=s.scrollLeft-dx;return}if(Math.hypot(dx,dy)>10){moved=true;clearTimeout(longTimer)}return}if(Math.hypot(dx,dy)>5)moved=true;if(moved&&!ghost)ghost=makeGhost(c,false,true);if(ghost){ghost.style.left=e.clientX+'px';ghost.style.top=e.clientY+'px'}const rr=row.getBoundingClientRect();if(e.clientX<rr.left+35)row.scrollLeft-=8;else if(e.clientX>rr.right-35)row.scrollLeft+=8};
 d.onpointerup=e=>{clearTimeout(longTimer);if(!s)return;d.releasePointerCapture?.(s.pid);s=null;ghost?.remove();ghost=null;if(scrollMode){scrollMode=false;return}if(selectedHand===id){if(trashHit(e.clientX,e.clientY)){deleteCards([id]);return}const b=fieldRect();if(pointInRect(e.clientX,e.clientY,b)&&!handHit(e.clientX,e.clientY)){selectedHand=null;placeOnField(id,e.clientX,e.clientY,false);render();return}const under=document.elementsFromPoint(e.clientX,e.clientY).find(x=>x.classList?.contains('hcard')&&x.dataset.id!==id);if(under){const from=st.hand.indexOf(id),to=st.hand.indexOf(under.dataset.id);st.hand.splice(from,1);st.hand.splice(to,0,id);selectedHand=null;render();toast('Hand reordered');return}selectedHand=null;render();return}if(!moved)openCard(c,true)};
 d.onpointercancel=()=>{clearTimeout(longTimer);s=null;ghost?.remove()};d.oncontextmenu=e=>e.preventDefault();return d
}
function renderPile(el,arr,label,{back=false,draggable=false,from=null,forceBack=false}={}){el.innerHTML='';el.classList.toggle('empty',!arr.length);const id=arr[0],c=id?st.cards[id]:null;if(c){if(back){const img=document.createElement('img');img.src=BACK;img.draggable=false;el.appendChild(img)}else el.insertAdjacentHTML('beforeend',face(c))}el.insertAdjacentHTML('beforeend',`<span class="count">${arr.length}</span>`);if(c&&draggable)zoneDrag(el,id,from,{forceBack,label});else el.onclick=()=>c?openCard(c,true):toast(label+' empty')}
function renderLife(){document.querySelectorAll('.life-heart[data-life]').forEach(el=>el.querySelector('span').textContent=st.life[el.dataset.life])}
function render(){
 const f=$('#field'),h=$('#handrow'),o=$('#oppcards'),ff=$('#fullcards');f.innerHTML=h.innerHTML=o.innerHTML=ff.innerHTML='';
 st.field.forEach(id=>f.appendChild(cardEl(st.cards[id],true)));st.opp.forEach(id=>o.appendChild(cardEl(st.cards[id],false)));
 st.field.forEach(id=>{const s=st.cards[id],copy={...s,x:18+s.x*.62,y:54+s.y*.32};ff.appendChild(cardEl(copy,false))});st.opp.forEach(id=>{const s=st.cards[id],copy={...s,x:18+s.x*.62,y:4+s.y*.32};ff.appendChild(cardEl(copy,false))});
 st.hand.forEach(id=>h.appendChild(handCard(id)));$('#hand').classList.toggle('empty',!st.hand.length);
 const ca=$('#cmds');ca.innerHTML='';if(!st.cmd.length){const e=document.createElement('div');e.className='cmd';e.dataset.name='COMMANDER';e.dataset.icon='♛';e.innerHTML='<span class="count">0</span>';ca.appendChild(e)}else st.cmd.slice(0,2).forEach(id=>{const c=st.cards[id],e=document.createElement('div');e.className='cmd';e.dataset.name='COMMANDER';e.dataset.icon='♛';e.innerHTML=face(c)+'<span class="count">1</span>';ca.appendChild(e);zoneDrag(e,id,'cmd',{label:'Commander to battlefield'})});
 renderPile($('#discard'),st.discard,'Graveyard',{draggable:true,from:'discard'});renderPile($('#deck'),st.deck,'Deck',{back:true,draggable:true,from:'deck',forceBack:true,label:'Drew face-down to battlefield'});renderPile($('#exile'),st.exile,'Exile',{draggable:true,from:'exile'});renderPile($('#tokens'),st.tokens,'Tokens',{draggable:true,from:'tokens',label:'Token to battlefield'});renderLife()
}
function expand(items,z){const out=[];(items||[]).forEach(x=>{const q=typeof x==='string'?1:+x.quantity||1,n=typeof x==='string'?x:x.name;for(let i=0;i<q;i++)out.push(make(n,z,typeof x==='string'?{}:x))});return out}
function applyDeck(d){
 st.cards=Object.fromEntries(Object.entries(st.cards).filter(([_,c])=>c.zone==='opp'));st.field=[];st.hand=[];st.discard=[];st.exile=[];st.side=[];selectedField.clear();selectedHand=null;
 st.cmd=expand(d.commander,'cmd');st.deck=expand(d.deck,'deck');st.deck.forEach(id=>st.cards[id].faceDown=true);st.tokens=expand(d.tokens,'tokens');
 const sideIds=expand((d.sideboard||[]).map(x=>typeof x==='string'?x:{...x,sideboard:true}),'field');sideIds.forEach((id,i)=>{const c=st.cards[id];c.zone='field';c.x=78+(i%3)*2.2;c.y=73+Math.floor(i/3)*1.6;st.field.push(id)});
 [...st.cmd.slice(0,2),...sideIds.slice(0,4),...st.tokens.slice(0,6)].filter(Boolean).forEach(id=>load(st.cards[id]).then(render));render();toast('Deck imported')
}
async function importDeck(){const r=await fetch('/api/import-archidekt',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url:$('#url').value.trim()})});let d;try{d=await r.json()}catch{throw Error('Importer returned an invalid response')}if(!r.ok)throw Error((d.error||'Import failed')+(d.detail?' · '+d.detail:''));applyDeck(d)}
function parseList(t){const r={commander:[],deck:[],sideboard:[],tokens:[]};let z='deck';t.split(/\r?\n/).forEach(l=>{l=l.trim();if(!l)return;const h=l.toLowerCase().replace(/:$/,'');if(/^commander/.test(h)){z='commander';return}if(/^sideboard/.test(h)){z='sideboard';return}if(/^tokens?/.test(h)){z='tokens';return}const m=l.match(/^(\d+)\s*x?\s+(.+)$/i);if(m)r[z].push({name:m[2],quantity:+m[1],commander:z==='commander',token:z==='tokens',sideboard:z==='sideboard'})});return r}
function shuffle(a){for(let i=a.length-1;i;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}}
async function drawOne(){if(!st.deck.length)return toast('Deck empty');const id=st.deck.shift(),c=st.cards[id];c.zone='hand';c.faceDown=false;st.hand.push(id);await load(c);render();toast('Drew 1')}
async function mulligan(){if(!st.deck.length&&!st.hand.length)return toast('Import a deck first');while(st.hand.length){const id=st.hand.pop();st.cards[id].zone='deck';st.cards[id].faceDown=true;st.deck.push(id)}shuffle(st.deck);const loads=[];for(let i=0;i<7&&st.deck.length;i++){const id=st.deck.shift(),c=st.cards[id];c.zone='hand';c.faceDown=false;st.hand.push(id);loads.push(load(c))}await Promise.all(loads);selectedHand=null;$('#hand').classList.remove('open');render();toast('Shuffled · drew 7')}
function untapAll(){let changed=0;st.field.forEach(id=>{if(st.cards[id].tap){st.cards[id].tap=false;changed++}});render();toast(changed?`Untapped ${changed}`:'Everything already untapped')}
async function movePreviewTo(zone){if(!preview)return;const c=preview;putInZone(c.id,zone,zone==='deck');revealForZone(c,zone);if(!c.faceDown&&!c.img)await load(c);$('#inspect').classList.remove('on');render()}
document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{selectedField.clear();selectedHand=null;document.querySelectorAll('[data-v]').forEach(x=>x.classList.remove('on'));b.classList.add('on');document.querySelectorAll('.screen').forEach(x=>x.classList.remove('on'));$('#'+b.dataset.v).classList.add('on');st.view=b.dataset.v;if(st.view==='opp')$('#hand').classList.remove('open');render()});
document.querySelectorAll('.life-heart[data-life]').forEach(btn=>{let timer=null,last=0;btn.addEventListener('click',e=>{e.preventDefault();const k=btn.dataset.life,now=Date.now();if(now-last<330){clearTimeout(timer);last=0;st.life[k]++;renderLife();return}last=now;timer=setTimeout(()=>{if(last===now){st.life[k]--;last=0;renderLife()}},330)});btn.addEventListener('contextmenu',e=>e.preventDefault())});
$('#draw').onclick=drawOne;$('#mulligan').onclick=mulligan;$('#untap').onclick=untapAll;$('#closehand').onclick=()=>{selectedHand=null;$('#hand').classList.remove('open');render()};$('#import').onclick=()=>$('#imp').classList.add('on');$('#cancel').onclick=()=>$('#imp').classList.remove('on');$('#urlm').onclick=()=>{$('#urlm').classList.add('on');$('#listm').classList.remove('on');$('#urlp').classList.add('on');$('#listp').classList.remove('on')};$('#listm').onclick=()=>{$('#listm').classList.add('on');$('#urlm').classList.remove('on');$('#listp').classList.add('on');$('#urlp').classList.remove('on')};$('#run').onclick=async()=>{try{$('#msg').textContent='Importing…';$('#urlp').classList.contains('on')?await importDeck():applyDeck(parseList($('#list').value));$('#msg').textContent='Imported';setTimeout(()=>$('#imp').classList.remove('on'),600)}catch(e){$('#msg').textContent=e.message}};
$('#inspect').onclick=e=>{if(e.target===$('#inspect'))$('#inspect').classList.remove('on')};$('#pimg').onclick=e=>e.stopPropagation();$('#tap').onclick=()=>{if(preview){preview.tap=!preview.tap;render();rerenderPreview()}};$('#one').onclick=()=>{if(preview){preview.p1++;render();rerenderPreview()}};$('#manual').onclick=()=>$('#xx').classList.toggle('on');$('#apply').onclick=()=>{if(preview){preview.p=parseInt($('#px').value)||0;preview.t=parseInt($('#tx').value)||0;$('#xx').classList.remove('on');render();rerenderPreview()}};$('#flip').onclick=()=>{if(preview){preview.faceDown=!preview.faceDown;rerenderPreview();render()}};$('#state').onclick=()=>{if(preview?.isDoubleFaced){preview.stateIndex=(preview.stateIndex+1)%preview.faces.length;rerenderPreview();render()}};$('#tohand').onclick=()=>movePreviewTo('hand');$('#todiscard').onclick=()=>movePreviewTo('discard');$('#toexile').onclick=()=>movePreviewTo('exile');
$('#trash')?.addEventListener('click',()=>{if(selectedField.size)deleteCards([...selectedField]);else if(selectedHand)deleteCards([selectedHand]);else toast('Lock a card or pile first')});
document.addEventListener('contextmenu',e=>{if(!e.target.closest('#imp'))e.preventDefault()},{capture:true});document.addEventListener('selectstart',e=>{if(!e.target.closest('#imp'))e.preventDefault()},{capture:true});document.addEventListener('dragstart',e=>e.preventDefault());document.addEventListener('keydown',e=>{if(e.target&&/input|textarea/i.test(e.target.tagName))return;if(e.key==='h'||e.key==='H')$('#hand').classList.toggle('open');if(e.key==='d'||e.key==='D')drawOne();if(e.key==='u'||e.key==='U')untapAll();if(e.key==='Escape'){selectedField.clear();selectedHand=null;$('#inspect').classList.remove('on');$('#imp').classList.remove('on');$('#hand').classList.remove('open');render()}});
['Shivan Dragon','Birds of Paradise','Counterspell'].forEach((n,i)=>{const id=make(n,'opp');st.cards[id].x=24+i*25;st.cards[id].y=20;st.opp.push(id);load(st.cards[id]).then(render)});render();
})();