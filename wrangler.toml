name = "mtg-tabletop-cloudflarefinal"
main = "worker-h41.js"
compatibility_date = "2026-08-31"

# H41: keep the stable H40 lightweight battlefield viewer and add direct
# card controls without reconnecting the old observer-heavy inspect stack.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
