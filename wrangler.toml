name = "mtg-tabletop-cloudflarefinal"
main = "worker-h46.js"
compatibility_date = "2026-08-31"

# H46: keep the stable H45 viewer and compact battlefield badge while showing
# one combined P/T value when X/X and +1/+1 counters are both active.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
