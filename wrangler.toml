name = "mtg-tabletop-cloudflarefinal"
main = "worker-h57.js"
compatibility_date = "2026-08-31"

# H57: explicitly stack the private Scry tray above a temporarily expanded
# Hand tray so both remain visible together while Scry cards exist.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
