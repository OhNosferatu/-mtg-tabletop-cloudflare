name = "mtg-tabletop-cloudflarefinal"
main = "worker-h183.js"
compatibility_date = "2026-08-31"

routes = [
  { pattern = "mtgtabletop.app", custom_domain = true }
]

# H183: stability rollback. Bypass H181/H182 board-height experiments and
# restore the last known-good H180 Full Board-only geometry and action controls.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
