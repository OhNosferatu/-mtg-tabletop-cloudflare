name = "mtg-tabletop-cloudflarefinal"
main = "worker-h178.js"
compatibility_date = "2026-08-31"

routes = [
  { pattern = "mtgtabletop.app", custom_domain = true }
]

# Rollback to H178: restore the last three-screen layout with Your Side,
# Full Board, and Opponent before the Full Board-only experiments began.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
