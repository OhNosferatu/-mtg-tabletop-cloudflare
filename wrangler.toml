name = "mtg-tabletop-cloudflarefinal"
main = "worker-h59.js"
compatibility_date = "2026-08-31"

# H59: while Scry is active, show only a compact Hand peek strip beneath it
# and hide Hand open/close and scrollbar controls until Scry is resolved.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
