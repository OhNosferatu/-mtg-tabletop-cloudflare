name = "mtg-tabletop-cloudflarefinal"
main = "worker.js"
compatibility_date = "2026-08-31"

# Build marker H20: force a fresh Workers Build so the H19 hand-close fix reaches production.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
