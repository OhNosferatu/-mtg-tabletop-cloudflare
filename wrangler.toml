name = "mtg-tabletop-cloudflarefinal"
main = "worker-h54.js"
compatibility_date = "2026-08-31"

# H54: keep H53's stable isolated deck menu while making Scry Hand cards
# move into the player's hand or battlefield using the normal hand destinations.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
