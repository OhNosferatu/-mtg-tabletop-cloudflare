name = "mtg-tabletop-cloudflarefinal"
main = "worker-h43.js"
compatibility_date = "2026-08-31"

# H43: keep the stable lightweight viewer, move counter readouts onto the card,
# shrink the zoomed card, and make the overlay translucent.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
