name = "mtg-tabletop-cloudflarefinal"
main = "worker-h56.js"
compatibility_date = "2026-08-31"

# H56: keep H55 interaction fixes while positioning the private Scry tray
# directly above the real Hand tray with matching width and separate layout.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
