name = "mtg-tabletop-cloudflarefinal"
main = "worker-h42.js"
compatibility_date = "2026-08-31"

# H42: retain the stable lightweight battlefield viewer, restore the original
# split +1/+1 and press/hold X/X behaviors, and evenly space left-side piles.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
