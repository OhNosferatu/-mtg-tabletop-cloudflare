name = "mtg-tabletop-cloudflarefinal"
main = "worker-h69.js"
compatibility_date = "2026-08-31"

# H69: deploy only the proven H64 build-time patch chain, then apply newer
# deck behavior at runtime when serving app.js.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
