name = "mtg-tabletop-cloudflarefinal"
main = "worker-h68.js"
compatibility_date = "2026-08-31"

# H68: deploy directly from the known-good H64 baseline and consolidate the
# Reveal/Unreveal, orientation-preserving Flip Deck, and persistent Draw/Scry fixes.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
