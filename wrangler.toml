name = "mtg-tabletop-cloudflarefinal"
main = "worker-h44.js"
compatibility_date = "2026-08-31"

# H44: keep H43's stable translucent lightweight viewer while moving the
# counter badge to the lower-right and showing base P/T without an X/X label.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
