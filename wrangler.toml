name = "mtg-tabletop-cloudflarefinal"
main = "worker.js"
compatibility_date = "2026-08-31"

# Build marker H19: forces a fresh Workers Build for the hand interaction update.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
