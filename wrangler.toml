name = "mtg-tabletop-cloudflarefinal"
main = "worker-h49.js"
compatibility_date = "2026-08-31"

# H49: keep the stable H48 deck controls, make Mill physically move/reveal the
# top card in Discard, and add a private Scry Hand above the player's hand.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
