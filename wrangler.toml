name = "mtg-tabletop-cloudflarefinal"
main = "worker-h39.js"
compatibility_date = "2026-08-31"

# H39: keep the reliable visible asset loader and deploy the iOS card-interaction
# stability patch before Wrangler scans/uploads public/app.js.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
