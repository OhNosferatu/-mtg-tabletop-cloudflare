name = "mtg-tabletop-cloudflarefinal"
main = "worker-h177.js"
compatibility_date = "2026-08-31"

routes = [
  { pattern = "mtgtabletop.app", custom_domain = true }
]

# H177: preserve H176 bounded Full Board scrolling. Align Full Board cards using
# each owner's exact board rectangle, remember Full Board scroll position, remove
# the temporary green boundary rule, unify pile-label styling, and restore the
# Your Side pile order: Commander -> Exile -> Deck -> Graveyard -> Tokens.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
