name = "mtg-tabletop-cloudflarefinal"
main = "worker-h61.js"
compatibility_date = "2026-08-31"

# H61: make the rendered top-card image authoritative on the Deck pile and
# recalculate the custom Hand scrollbar after Mulligan rebuilds the hand.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
