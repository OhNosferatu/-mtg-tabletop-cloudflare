name = "mtg-tabletop-cloudflarefinal"
main = "worker-h47.js"
compatibility_date = "2026-08-31"

# H47: keep the stable lightweight viewer, fix deck fullscreen exit/backdrop,
# and make deck actions update state exactly once per button press.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
