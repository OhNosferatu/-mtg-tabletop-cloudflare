name = "mtg-tabletop-cloudflarefinal"
main = "worker-h79.js"
compatibility_date = "2026-08-31"

routes = [
  { pattern = "mtgtabletop.app", custom_domain = true }
]

# H79: keep H71 complete token discovery, remove post-import render races,
# use the stable battlefield viewer for draggable piles, repair Flip, and make
# Mulligan collect the full player table state before drawing seven.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
