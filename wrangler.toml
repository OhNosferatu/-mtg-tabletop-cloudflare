name = "mtg-tabletop-cloudflarefinal"
main = "worker-h63.js"
compatibility_date = "2026-08-31"

# H63: draw new cards to the far-left of Hand and route Hand-card viewing
# through the stable lightweight full-card viewer instead of legacy inspect.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
