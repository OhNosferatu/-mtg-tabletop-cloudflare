name = "mtg-tabletop-cloudflarefinal"
main = "worker-h67.js"
compatibility_date = "2026-08-31"

# H67: keep the deck overlay open for Draw and Scry actions, then surface
# the Hand or private Scry tray only when the player exits the deck menu.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
